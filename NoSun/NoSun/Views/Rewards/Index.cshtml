@model IEnumerable<NoSun.Models.Reward>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RewardDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model._NPC.NPCName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RewardDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item._NPC.NPCName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RewardID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RewardID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RewardID })
        </td>
    </tr>
}

</table>
